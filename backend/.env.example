# LLM keys
OPENAI_API_KEY=sk-...
OPENROUTER_API_KEY=sk-or-v1-...

# Optional runtime settings
BACKEND_LOG_LEVEL=INFO
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0
BACKEND_AUTO_RELOAD=1

# Optional CORS overrides (comma-separated full origins)
# BACKEND_CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:20225

# Optional regex override
# BACKEND_CORS_ALLOW_ORIGIN_REGEX=https?://[^/]+(?::(3000|5173|20221|20225))?$

# OCR parser runtime isolation (recommended to avoid transformers conflicts)
# Choose one:
# DLA_PARSER_PYTHON=/workspace/dla/venv_ocr/bin/python
# DLA_PARSER_VENV=venv_ocr
# Runtime mode: worker(default, fast) | single(debug)
# DLA_PARSER_MODE=worker
# Optional timeout (seconds) for one page parse
# DLA_PARSER_TIMEOUT_SEC=420
# Optional worker init timeout (seconds)
# DLA_PARSER_INIT_TIMEOUT_SEC=180
