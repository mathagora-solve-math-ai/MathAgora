# Backend API base URL for detect/classify and solve.
# In dev (npm run dev), if unset, defaults to http://localhost:8000 (miniserver).
# Set to your backend origin, e.g. when using backend/miniserver.py locally:
#   VITE_DETECT_API_URL=http://localhost:8000
#   VITE_SOLVE_API_URL=http://localhost:8000
# VITE_DETECT_API_URL=
# VITE_SOLVE_API_URL=
